# День 4: Індексація в SQL, оптимізація запитів та EXPLAIN ANALYZE

Сьогодні ми занурюємося в продуктивність баз даних. Ти навчишся розуміти, як працює планувальник запитів PostgreSQL, чому таблиці раптово «гальмують» на великих обсягах даних, як правильно підбирати типи індексів та проєктувати складені (composite) індекси в ORM.

---

## ⏱️ Розклад Дня 4 (6 годин)

| Блок       | Тривалість | Тема                                 | Опис                                                                                              |
| :--------- | :--------- | :----------------------------------- | :------------------------------------------------------------------------------------------------ |
| **Блок 1** | 1 год      | Алгоритмічний розігрів (TS/JS)       | Бінарний пошук та індексація масиву в пам'яті: $O(N)$ vs $O(\log N)$ vs $O(1)$                    |
| **Блок 2** | 2.5 год    | Дослідження планувальника PostgreSQL | Генерація 200,000+ записів, `EXPLAIN (ANALYZE, BUFFERS)`, `Seq Scan`, `Index Scan`, `Bitmap Scan` |
| **Блок 3** | 1.5 год    | Практика створення індексів в ORM    | B-Tree індекси, складені індекси (правило лівого префікса), часткові (partial) індекси            |
| **Блок 4** | 1 год      | Рев'ю та інтерв'ю-підготовка         | B-Tree структура, ціна індексів на операціях запису, Cardinality та selectivity                   |

---

## 🧩 Завдання 1: Алгоритмічний розігрів (Індексований пошук у TypeScript)

### Мета

Зрозуміти принцип роботи індексу в базі даних шляхом реалізації структури для швидкого пошуку об'єктів за кількома критеріями.

### Вхідні дані

Масив товарів з великою кількістю записів:

```typescript
export type Product = {
  id: string;
  category: string;
  price: number;
  inStock: boolean;
};
```

### Вимоги

1. **Лінійний пошук ($O(N)$):**
   Напиши базову функцію `findProducts(products: Product[], category: string, inStock: boolean, minPrice: number, maxPrice: number): Product[]`.
2. **Власний композитний індекс ($O(1)$ lookup + фільтрація діапазону):**
   Створи клас `ProductIndex`:
   - Приймає масив `Product[]` у конструкторі та будує хеш-індекс за ключем `category:inStock`.
   - Значеннями в мапі мають бути списки товарів, відсортовані за `price`.
   - Реалізуй метод `query(category: string, inStock: boolean, minPrice: number, maxPrice: number): Product[]`, який:
     - Отримує вже відфільтровану категорію за $O(1)$;
     - Використовує бінарний пошук для знаходження діапазону цін замість проходу по всьому масиву.
3. Порівняй час виконання на вибірці з 50 000 згенерованих об'єктів.

---

## 🔍 Завдання 2: Експерименти з PostgreSQL та EXPLAIN ANALYZE

### Мета

Навчитися читати плани виконання запитів і бачити різницю між послідовним скануванням та використанням індексів.

### Кроки

1. **Генерація великого обсягу даних:**
   Утилітою SQL згенеруй 200,000 записів у таблицю `orders` (через pgAdmin або CLI):

   ```sql
   INSERT INTO "Order" ("id", "userId", "status", "totalAmount", "createdAt")
   SELECT
     gen_random_uuid(),
     (SELECT "id" FROM "User" ORDER BY random() LIMIT 1),
     (ARRAY['PENDING', 'PAID', 'SHIPPED', 'CANCELLED'])[floor(random() * 4 + 1)],
     floor(random() * 10000 + 100)::int,
     NOW() - (random() * (interval '90 days'))
   FROM generate_series(1, 200000);
   ```

2. **Аналіз вихідного запиту:**
   Виконай запит без індексів:

   ```sql
   EXPLAIN (ANALYZE, BUFFERS)
   SELECT * FROM "Order"
   WHERE "status" = 'PAID' AND "createdAt" >= NOW() - INTERVAL '7 days'
   ORDER BY "createdAt" DESC;
   ```

   Зверни увагу на:
   - Тип сканування (`Seq Scan`);
   - Кількість прочитаних сторінок (`Buffers: shared hit/read`);
   - Загальний час (`Execution Time`).

---

## ⚡ Завдання 3: Проєктування та накатка індексів

### Мета

Оптимізувати запити за допомогою правильних B-Tree індексів через Prisma або TypeORM.

### Кроки

1. **Складений індекс (Composite Index):**
   - Додай складений індекс на поля `(status, createdAt)` або `(userId, createdAt)` у схемі ORM.
   - Згенеруй і застосуй міграцію.
   - Повторно запусти `EXPLAIN (ANALYZE, BUFFERS)`.
   - Зафіксуй зміну: чи з'явився `Index Scan` або `Bitmap Index Scan`? Як змінився Execution Time?
2. **Перевірка правила лівого префікса (Leftmost Prefix Rule):**
   - Запусти запит, який фільтрує лише за `createdAt`, коли індекс створено як `(status, createdAt)`.
   - Поясни, чому індекс може ігноруватися базою даних у цьому випадку.
3. **Частковий індекс (Partial Index):**
   - Створи індекс, який зберігає записи тільки для замовлень зі статусом `PENDING` (наприклад, для черги необроблених замовлень):

     ```sql
     CREATE INDEX idx_orders_pending ON "Order" ("createdAt") WHERE "status" = 'PENDING';
     ```

   - Порівняй розмір такого індексу з повним індексом.

---

## 🎯 Питання для самоперевірки та інтерв'ю

1. **Що таке B-Tree індекс?** Чому він є стандартом за замовчуванням у реляційних БД і за яку асимптотичну складність він виконує пошук, вставку та діапазонні запити?
2. **Чим `Index Scan` відрізняється від `Bitmap Index Scan` та `Index Only Scan`?**
3. **Правило лівого префікса:** Якщо індекс створено на `(A, B, C)`, які комбінації в блоці `WHERE` зможуть його використати, а які — ні?
4. **Ціна індексів:** Якщо індекси так прискорюють читання, чому ми просто не індексуємо кожну колонку таблиці?
